name: Docker Image CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Login to registry
      run: docker login -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }} ghcr.io
    - name: Setup buildx plugin
      run: |
        docker run --rm --privileged docker/binfmt:820fdd95a9972a5308930a2bdfb8573dd4447ad3
        mkdir -p ~/.docker/cli-plugins/
        wget https://github.com/docker/buildx/releases/download/v0.5.1/buildx-v0.5.1.linux-amd64 -O ~/.docker/cli-plugins/docker-buildx
        chmod a+x ~/.docker/cli-plugins/docker-buildx
        docker buildx create --name xbuilder --use
        docker buildx inspect --bootstrap
    - name: Build the Docker image
      run: docker buildx build --platform linux/arm/v7,linux/arm64,linux/amd64 -t docker.pkg.github.com/${{ github.repository }}/opensuse-salt:latest --push .
